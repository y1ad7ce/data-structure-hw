HOMEWORK 10: JOB PRIORITIZATION







Calling MinHeapify every time is somewhat inefficient. In the worst case, heapify is O(n). However, when removing from index, just percolate elements up or down to find its place in O(log n) time.